---
name: to-issues
description: Разбить PRD на независимые но взаимосвязанные задачи в папке проекта через вертикальные задачи (tracer bullets). Используется, когда пользователь хочет превратить PRD в задачи.
---

# To Issues

Разбить план на независимо выполнимые задачи через вертикальные задачи (tracer bullets).

## Процесс

### 1. Собери контекст

Работай с тем, что уже есть в контексте диалога. Если пользователь передал роут на документ как аргумент — прочитай его целиком.

### 2. Изучи кодовую базу (опционально)

Если ты ещё не изучил кодовую базу — сделай это, чтобы понять текущее состояние кода. Заголовки и описания issue должны использовать словарь доменной области проекта и уважать ADR (architectural decision records) в рабочей части кода.

### 3. Проектируй вертикальные задачи

Разбей план на задачи-**tracer bullets**. Каждая задача — это тонкий вертикальный путь через ВСЕ слои интеграции приложения, а НЕ горизонтальный пласт одного слоя репозитория.

Задачи бывают 'HITL' или 'AFK'. HITL-задачи требуют участия человека — например, архитектурного решения или ревью дизайна. AFK-задачи можно реализовать без участия человека. По возможности предпочитай AFK поверх HITL.

<vertical-slice-rules>
- Каждая задача доставляет изолированный, но ПОЛНЫЙ путь через все слои (схема, API, UI, тесты)
- Собранную задачу можно продемонстрировать или верифицировать самостоятельно
- Предпочитай много изолированных компактных задач, малому количеству больших размытых задач.
</vertical-slice-rules>

### 4. Опроси пользователя

Представь предлагаемую разбивку как нумерованный список. Для каждой задачи покажи:

- **Заголовок**: короткое описательное название
- **Тип**: HITL / AFK
- **Блокируется**: какие другие задачи (если есть) должны быть завершены раньше
- **Покрываемые user stories**: какие пользовательские истории закрывает задача (если в исходных материалах они есть)

Спроси пользователя:

- Подходит ли гранулярность? (слишком крупно / слишком мелко)
- Корректны ли зависимости между задачами?
- Нужно ли какие-то задачи объединить или раздробить дальше?
- Правильно ли расставлены метки HITL и AFK?

Итерируй, пока пользователь не утвердит разбивку.

### 5. Опубликуй issue в трекер

Для каждой утверждённой задачи опубликуй новую issue в трекер. Используй шаблон тела issue ниже. Эти задачи считаются готовыми для AFK-агентов, поэтому публикуй их с правильным триаж-лейблом, если не указано иное.

Публикуй задачи в порядке зависимостей (сначала блокирующие), чтобы в поле «Блокируется» можно было сослаться на реальные идентификаторы issue.

<issue-template>
## Родительская задача

Ссылка на родительскую задачу в трекере (если исходником была существующая issue, иначе раздел опусти).

## Что строим

Краткое описание этой вертикальной задачи. Описывай end-to-end поведение, а не послойную реализацию.

Избегай конкретных путей к файлам и фрагментов кода — они быстро устаревают. Исключение: если прототип произвёл фрагмент, который кодирует решение точнее, чем проза (state machine, reducer, схема, форма типа), — вставь его сюда и кратко отметь, что он пришёл из прототипа. Оставь только те части, где принимаются решения, — не рабочий демо, а только важные куски.

## Критерии выполненной задачи

- [ ] Критерий 1
- [ ] Критерий 2
- [ ] Критерий 3

## Блокируется

- Ссылка на блокирующий тикет (если есть)

Или «Нет — можно начинать сразу», если блокеров нет.

</issue-template>

НЕ закрывай и НЕ изменяй родительскую issue.
